def send_push_notification(to: str, title: str, message: str, **kwargs) -> None:
    """
    Stub for sending a push notification. Replace with actual implementation.
    """
    print(f"Sending push notification to {to}: {title} - {message}")